import { Box, styled } from '@mui/material';

export const BannerContainer = styled(Box)(({ theme }) => ({
  height: '420px',
  width: '100%',
  position: 'relative',
  borderRadius: '10px',
  overflow: 'hidden',
}));
