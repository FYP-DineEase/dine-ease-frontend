import { Box, styled } from '@mui/material';

export const BannerContainer = styled(Box)(({ theme }) => ({
  height: '450px',
  width: '100%',
  position: 'relative',
  borderRadius: '12px',
  overflow: 'hidden',
}));
