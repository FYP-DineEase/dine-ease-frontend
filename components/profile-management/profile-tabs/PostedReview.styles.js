import { Box, styled } from "@mui/material";

export const PostedReviewContainer = styled(Box)(({ theme }) => ({
  display: "flex",
  gap: "2rem",
  flexDirection: "column",
  padding: "4rem 8vw",
}));