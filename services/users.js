import api from './api';
import { PORTS } from '@/utils/port';

const service = 'users';
const port = PORTS[service];

export const getUserDetails = () => {
  // fetch user by token
};

export const getUserById = (userId) => {
  // fetch user by short id
};
